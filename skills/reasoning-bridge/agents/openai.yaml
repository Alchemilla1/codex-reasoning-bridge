interface:
  display_name: "Reasoning Bridge"
  short_description: "Ask a ChatGPT web advisor for a high-level decision"
  default_prompt: "Use $reasoning-bridge to ask the ChatGPT web advisor for a researched direction, then return the answer to the active task."
policy:
  allow_implicit_invocation: false
