interface:
  display_name: "Reasoning Bridge"
  short_description: "Advise a remote Codex task through ChatGPT web"
  default_prompt: "Use $reasoning-bridge with a remote thread ID to read that task, ask the ChatGPT web advisor, and return the answer to the same remote task."
policy:
  allow_implicit_invocation: false
